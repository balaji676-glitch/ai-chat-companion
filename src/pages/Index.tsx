import ChatWindow from "@/components/ChatWindow";

const Index = () => (
  <main className="flex min-h-dvh items-center justify-center bg-gradient-to-br from-[hsl(var(--chat-gradient-from)/0.08)] via-background to-[hsl(var(--chat-gradient-to)/0.08)] p-4">
    <ChatWindow />
  </main>
);

export default Index;
